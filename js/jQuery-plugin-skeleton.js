/* /////////////////////////////////////////////
Put your description of the plugin here
@author [author]
@url [webpage]
@license [webpage]
///////////////////////////////////////////// */

(function( $ ) {
   
   	/**
  	 * Put your jQueryu plugin javascript coding here
  	 */
	
	/**
  	 * Example: 
  	 *  $.fn.popup = function( action ) {
     *
     *   if ( action === "open") {
     *       // Open popup code.
     *   }
 	 *	
     *   if ( action === "close" ) {
     *       // Close popup code.
     *   }
     *	};
   	 */
}( jQuery ));


// To call the popup from inline code
// $( "a" ).popup();